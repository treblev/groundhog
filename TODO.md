# Groundhog — TODO

- Add a deep_agents-style mutable planning tool (`write_todos`) to `langgraph_client/client.py` — a tool the agent calls and revises mid-loop as new observations come in, instead of the old `mcp_client`'s frozen upfront plan (which caused the day_of_week/category bug: the plan guessed wrong once and was never revisited after the tool result contradicted it). Don't pull in the full `deepagents` framework — it's built for long-horizon multi-step tasks (sub-agents, virtual file system, persistent cross-session memory) that this project doesn't need; just add the planning-tool piece as a custom tool + middleware on top of the existing `create_agent()` setup.
- `langgraph_client/client.py`: add `ToolRetryMiddleware` (from `langchain.agents.middleware`) to catch malformed tool calls — `qwen3:32b` occasionally emits a tool call as literal text content (e.g. `{"name": "get_health_summary", "arguments": {}}`) instead of a structured `tool_calls` entry, and nothing currently catches it, so the agent silently returns the raw text as its final answer instead of retrying.
- Add replanning: right now neither client ever revisits a plan once made — `mcp_client`'s plan is generated once with `tools=None` (pure guesswork off schema text, no way to verify) and then treated as settled for the rest of the conversation; `langgraph_client` has no plan artifact at all to revisit. The `write_todos` tool above gives the agent a place to *store* a revisable plan, but doesn't by itself force it to reconsider — worth explicitly prompting/steering the agent to re-check its plan against each new tool observation (e.g. "after every tool result, ask whether it contradicts your current plan before proceeding") rather than assuming a mutable todo list alone fixes the day_of_week/category-style bug.
