# 2026-05-14 — Agentic Tool Calling

## What was built
- Rewrote `agent/query.py` as a proper agentic loop with tool calling
- Defined 4 tools: `run_sql`, `get_latest_price`, `get_recent_activities`, `get_health_summary`
- Added fallback parser for models that output tool calls as JSON text instead of structured `tool_calls` field
- Fixed Qwen special token leaking issue

## How it works
Instead of always generating SQL, the model is given a set of tools and decides which one to call. The loop runs up to 5 rounds — model calls a tool, we execute it, feed the result back, model decides to call another tool or give a final answer.

## Problems faced
- **Model outputs tool call as JSON text, not structured `tool_calls`** — `qwen2.5-coder:32b` doesn't use the native `tool_calls` field. Instead it returns the tool call as JSON in `content`. Added a fallback that parses `content` as JSON if `tool_calls` is absent.
- **Qwen special tokens leaking into output** — the model appended `<|im_start|><|im_start|>` after the JSON, causing `json.loads()` to fail. Fixed by using greedy regex `\{.*\}` to extract just the JSON object and discard everything after the last `}`.

## Concepts touched
- Agentic loops with tool calling
- Native vs fallback tool call parsing
- LLM special token bleed-through
- Greedy vs non-greedy regex for nested JSON extraction
